# 🏗️ Arquitectura de TextME

## Flujo General de la Aplicación

```
┌─────────────┐
│   Cliente   │
│ (Navegador) │
└──────┬──────┘
       │
       │ HTTP Request
       │
       ▼
┌──────────────────────────────┐
│      Express Server          │
│    (server.js)               │
├──────────────────────────────┤
│  - Middleware (cors, json)   │
│  - Rutas de la API           │
│  - Manejo de errores         │
└──────────────┬───────────────┘
               │
               │ Queries/Commands
               │
               ▼
┌──────────────────────────────┐
│  Mongoose (ODM)              │
│  (messageRoutes.js)          │
├──────────────────────────────┤
│  - Validación de datos       │
│  - Lógica de negocios        │
│  - Queries a BD              │
└──────────────┬───────────────┘
               │
               │ MongoDB Commands
               │
               ▼
┌──────────────────────────────┐
│      MongoDB Database        │
│    (NoSQL Document Store)    │
├──────────────────────────────┤
│  Colección: messages         │
│  - Documents JSON            │
│  - Índices optimizados       │
└──────────────────────────────┘
```

---

## Estructura de Directorios

```
project/
│
├── Models
│   └── Message.js
│       └── Schema: headers + body
│
├── Routes
│   └── messageRoutes.js
│       ├── GET /api/messages/user/:email
│       ├── GET /api/message/:id
│       └── POST /api/message
│
├── Server
│   └── server.js
│       ├── Express app
│       ├── MongoDB connection
│       └── Middleware setup
│
├── Utils
│   ├── seedDatabase.js (datos de ejemplo)
│   ├── test-api.js (pruebas)
│   └── examples.js (ejemplos)
│
└── Docs
    ├── README.md
    ├── DATABASE_DESIGN.md
    ├── QUICK_START.md
    └── PROJECT_STRUCTURE.md
```

---

## Flujo de Datos por Endpoint

### 1. GET /api/messages/user/:email

```
┌──────────────────────┐
│  Request HTTP        │
│  email: user@ex.com  │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  messageRoutes.js - router.get()     │
│  Línea ~10-35                        │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Message.find()                      │
│  Búsqueda OR:                        │
│  - headers.de = email                │
│  - headers.para = email              │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  MongoDB Query                       │
│  Usa índice: headers.de/para         │
│  Ordena: headers.stamp -1            │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Resultados: Solo headers            │
│  [{id, de, para, asunto, stamp}]    │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Response JSON                       │
│  {success, count, data: [...]}       │
└──────────────────────────────────────┘
```

---

### 2. GET /api/message/:id

```
┌──────────────────────┐
│  Request HTTP        │
│  id: msg_12345       │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  messageRoutes.js - router.get()     │
│  Línea ~37-65                        │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Message.findOne()                   │
│  Búsqueda por:                       │
│  - headers.id = id                   │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  MongoDB Query                       │
│  Usa índice: headers.id (unique)     │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Resultados: Headers + Body          │
│  {headers: {...}, body: {...}}       │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Response JSON                       │
│  {success, data: {headers, body}}    │
└──────────────────────────────────────┘
```

---

### 3. POST /api/message

```
┌─────────────────────────────────┐
│  Request HTTP Body              │
│  {de, para, asunto, contenido}  │
│  (adjunto, token: opcional)     │
└──────────┬──────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  messageRoutes.js - router.post()    │
│  Línea ~67-120                       │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Validación de campos                │
│  - Verifica: de, para, asunto,       │
│    contenido                         │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Generar ID único (UUID)             │
│  msg_550e8400-e29b-41d4-...         │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Crear objeto Message                │
│  {                                   │
│    headers: {...},                   │
│    body: {...}                       │
│  }                                   │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  message.save() - Insertar en BD    │
│  MongoDB: insertOne()                │
└──────────┬──────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Response JSON (Status 201)          │
│  {success, message, data: {...}}     │
└──────────────────────────────────────┘
```

---

## Schema de MongoDB

```
messages
│
├── _id: ObjectId (auto)
│
├── headers
│   ├── id: String (unique, indexed)
│   ├── de: String (indexed)
│   ├── para: String (indexed)
│   ├── asunto: String
│   └── stamp: Date (indexed, auto)
│
└── body
    ├── contenido: String
    ├── adjunto: String (null)
    └── token: String (null)
```

---

## Índices

```
│ Índice 1: headers.de + headers.stamp (-1)
│   → Rápido buscar mensajes enviados por usuario
│   → Ordenados por fecha descendente
│
│ Índice 2: headers.para + headers.stamp (-1)
│   → Rápido buscar mensajes recibidos por usuario
│   → Ordenados por fecha descendente
│
│ Índice 3: headers.id (unique)
│   → Búsqueda rápida por ID del mensaje
│   → Garantiza unicidad
```

---

## Ciclo de Vida de un Mensaje

```
1. CREAR
   ├── Usuario envía formulario
   ├── POST /api/message
   ├── Validar campos
   ├── Generar ID + timestamp
   └── Guardar en BD

2. LISTAR
   ├── Usuario accede a bandeja
   ├── GET /api/messages/user/:email
   ├── BD busca (de OR para = email)
   ├── Ordena por fecha descendente
   └── Retorna solo cabeceras

3. LEER
   ├── Usuario abre un mensaje
   ├── GET /api/message/:id
   ├── BD busca por ID
   └── Retorna mensaje completo

4. ARCHIVAR/ELIMINAR (Futuro)
   ├── PATCH /api/message/:id
   ├── DELETE /api/message/:id
   └── (No implementado en Parcial 1)
```

---

## Stack de Tecnologías

```
┌─────────────────────────────────┐
│  FRONTEND (Futuro)              │
│  - HTML/CSS/JavaScript          │
│  - React/Vue/Angular            │
└──────────┬──────────────────────┘
           │ JSON
           │
┌──────────▼──────────────────────┐
│  BACKEND                        │
│  Node.js + Express              │
└──────────┬──────────────────────┘
           │ MongoDB Commands
           │
┌──────────▼──────────────────────┐
│  DATABASE                       │
│  MongoDB (NoSQL)                │
│  - Collections                  │
│  - Documents                    │
│  - Índices                      │
└─────────────────────────────────┘
```

---

## Seguridad (Implementado/Futuro)

```
✅ Validación de campos requeridos
✅ CORS configurado
✅ Express JSON parser (previene inyección)
⏳ Campo token preparado para autenticación
⏳ Rate limiting (futuro)
⏳ Encriptación de contraseñas (futuro)
⏳ HTTPS en producción (futuro)
```
